<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adaptive">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Adaptive">
        <activity android:name="com.example.adaptive.exercises.ExerciseActivity"></activity>
        <activity android:name="com.example.adaptive.workouts.WorkoutsActivity" />
        <activity android:name="com.example.adaptive.EmomActivity" />
        <activity android:name="com.example.adaptive.StopwatchActivity" />
        <activity android:name="com.example.adaptive.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>